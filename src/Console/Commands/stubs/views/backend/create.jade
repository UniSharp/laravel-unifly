extends ../scaffold/index
include ../partial/mixins

block main
  .container-fluid
    h1.page-header {{_('新增DummyEntity')}}
    include ../partial/breadcrumbs
    +form("{{route('backend.DummyEntityLowerCase.store')}}")
      +input('name', 'DummyEntity名稱')
      +textarea('description', '敘述')
      +number('quantity', '數量')
      +file('main_image', '主圖')
      +datetime('time', '時間')
      | <?php $types = ['類型一', '類型二'];?>
      | <?php $selected = 1;?>
      +select('type', '類型', "$types", "$selected")
      | <?php $options = ['option 1', 'option 2'];?>
      | <?php $checked = [0, 1];?>
      +checkbox('options', '多選選項', "$options", "$checked")
      | <?php $yn = [1 => '是', 0 => '否'];?>
      | <?php $on = 1;?>
      +radio('on', '是否上架', "$yn", "$on")

block javascript
  +datetimepicker
